[package]
name = "game-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
game-core = { path = "../game-core" }
wasm-bindgen = "0.2"
bincode = "1.3"

[profile.release]
opt-level = "s"
lto = true
